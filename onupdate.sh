#!/bin/bash
# TODO XXX FIXME do nothing now as the vim  castle shares vim plugins with neovim
# # we need the neovim files to be linked in order to avoid installing unnecessary
# # stuff and installing custom stuff
# # if the homeshick repo has an update, re-link the castle files before updating
# if [[ $1 == 1 ]]; then
#     homeshick=$HOME/.homesick/repos/homeshick/bin/homeshick
#     $homeshick link cfg-nvim
# fi
# 
# nvim "+PlugUpgrade" "+PlugUpdate" '+qa!'
